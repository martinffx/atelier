{
  "skill_name": "code-review",
  "evals": [
    {
      "id": 1,
      "prompt": "Can you review this TypeScript function? I think there might be a memory leak somewhere.\n\n```typescript\nclass UserService {\n  private cache = new Map<string, User>();\n  \n  async getUser(id: string): Promise<User> {\n    if (this.cache.has(id)) {\n      return this.cache.get(id)!;\n    }\n    \n    const user = await this.userRepo.findById(id);\n    this.cache.set(id, user);\n    return user;\n  }\n}\n```",
      "expected_output": "Should identify the memory leak (cache grows unbounded), suggest size limits or TTL, and provide the structured review format",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Please review my Python code for security issues. Here's the login handler:\n\n```python\ndef login(request):\n    username = request.json['username']\n    password = request.json['password']\n    \n    query = f\"SELECT * FROM users WHERE username = '{username}' AND password = '{password}'\"\n    result = db.execute(query)\n    \n    if result:\n        return {'token': create_token(result[0]['id'])}\n    return {'error': 'Invalid credentials'}\n```",
      "expected_output": "Should clearly identify SQL injection vulnerability, suggest parameterized queries, and maybe mention hashing passwords",
      "files": []
    },
    {
      "id": 3,
      "prompt": "What do you think of this code? Any issues or improvements?\n\n```javascript\n// In a React component\nuseEffect(() => {\n  const fetchData = async () => {\n    const response = await fetch('/api/data');\n    const json = await response.json();\n    setData(json);\n  };\n  fetchData();\n}, []);\n```",
      "expected_output": "Should identify missing error handling, no loading state, potential race conditions, and missing cleanup for component unmount",
      "files": []
    }
  ]
}
